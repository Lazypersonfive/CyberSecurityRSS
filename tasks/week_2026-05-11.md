# Week 2026-05-11 Execution Plan

## Main Goal
把 AIHOT 式质量架构从“计划”推进到“可观测、可回测、可调参”的实际流水线。优先做能直接提升日报质量和调试效率的部分，不先做对外 API/RSS/Skill。

## Priority Tasks
1. **Source Audit + Registry v1**
   - 每日自动生成 `reports/source_audit.md`。
   - 统计各板块 T1/T1.5/T2/unknown/Google News 分布。
   - 列出入选条目中的未登记源，作为次日人工审阅清单。
   - 目标：入选条目 `unknown_items=0`，新增源必须显式登记。
   - Status 2026-05-11: done. `reports/source_audit.md` 已接入 daily，本周 selected unknown 已归零。

2. **Security Source Quality Pass**
   - 基于 source audit 清理 security 的低质/泛资讯/历史归档源。
   - 把中文安全源分为中文官方、中文安全、中文公众号、社区。
   - 目标：security 每日 15 条中至少 6 条中文，并且不是低质中文硬凑。
   - Status 2026-05-11: done for v1. 已移除 security 中周报连续标记的低质/0 条目泛资讯源：Hacker News Frontpage、TechCrunch、The Verge、Darknet、Sploitus；中文源分类已由 `source_registry.yaml` + `source_profile` 透出。

3. **Deterministic Event Clustering v1**
   - 先做 CVE、规范化 URL、标题实体规则聚类。
   - LLM dedupe 继续作为兜底。
   - 目标：同一 CVE/同一发布不重复占位。
   - Status 2026-05-11: done. 新增 `story_clustering.py`，在 LLM dedupe 前做 CVE/规范化 URL/保守标题实体聚类；digest item 透出 `story_id`、`related_urls`、`related_count`，payload 透出 `clustering_stats`。

4. **Offline Eval v1**
   - 用最近 7-14 天 docs/digest/output 回放 source policy 和 clustering。
   - 输出旧/新策略差异、中文比例、Google/X/官网占比。
   - 目标：调权重前先看回测结果。
   - Status 2026-05-11: done. `reports/offline_eval.md` 已接入 daily，并修正中文源统计不能使用生成后的中文摘要。

5. **Multidimensional Scoring Preparation**
   - 定义每个板块 `score_dimensions` schema 和 final_score 权重配置。
   - 暂不一次性切生产，先通过 mock/dry-run 验证 schema。
   - Status 2026-05-11: done for v1. 新增 `scoring_policy.py` 和 `config.scoring`，digest item 透出 `score`、`dimension_score`、`final_score`、`score_breakdown`；最终 selection 已改为优先使用 deterministic `final_score`。
   - Status 2026-05-11 follow-up: production scoring prompt 已开始要求 `score_dimensions`，解析失败或后端未返回时仍回退旧 `score`，保证 daily 不因 schema 漂移中断。

6. **AIHOT Compare Tool**
   - 拉 AIHOT selected items 作为 AI 板块外部 benchmark。
   - 不作为生产主信源。
   - Status 2026-05-11: done as manual tool. `aihot_compare.py` 输出 `reports/aihot_compare.md`，不接入 daily，避免外部 API 失败影响生产。

7. **Board Target Retuning + GitHub Actions Validation**
   - 传统安全：每日 15 条，中文至少 6 条；已通过 GitHub Actions 验证 `selected=15, chinese=6`。
   - AI 安全：每日 10 条，宁缺毋滥；已补 Protect AI、Prompt Security、XSignals，当前 GitHub Actions 结果为 `selected=7`，仍需继续补高质量 AI 安全源。
   - AI 前沿：每日 15 条，中文约三分之一，arXiv 最多 2 条；已通过 GitHub Actions 验证 `selected=15, chinese=3, arxiv=2, x=4`。
   - 金融科技：每日 10 条；已补 Finextra、The Fintech Times、Finance Magnates 和中文金融科技 fallback，GitHub Actions 验证 `selected=10, chinese=3`。
   - Status 2026-05-11: done for v1. 最新成功 workflow: `25656509845`，daily commit `fa34159`。

8. **Site Ordering**
   - 站点展示顺序改为“最值得关注优先”：`final_score` 降序，随后按 `source_tier` 和 `source_kind` 排序。
   - 兼容历史 digest：没有 `final_score` 时按 `source_tier` / `source_kind` 兜底。
   - Status 2026-05-11: done. `site_builder.py` 重建最近 7 天 feed；`tests/test_regressions.py` 新增排序回归测试。最新 commit `96fe18f`。

9. **Human Feedback / Reward Loop Design**
   - 目标：每天对入选/未入选新闻做轻量人工反馈，逐步把“这条好/坏”的偏好转成可回放、可调参的 source/score policy。
   - Status 2026-05-11: design only. 先不实现生产逻辑；设计草案见 `tasks/feedback_loop_plan.md`。

## Findings From 2026-05-11 Work
- Security 中文源低不是单纯缺源：关闭跨日 raw 去重后，72h 模拟候选从 49 条变为 108 条，中文候选约 30 条。
- 之前 `source_profile` 会把生成后的中文摘要算作中文源，导致报告误判 AI/finance 几乎全中文；已修复为只看原始标题、源域名和 registry。
- AIHOT 对比显示我们的 AI 板块更偏“工程/模型/安全”，AIHOT 更偏“选题/产品/投资/X 热点”；后续要靠 source registry 和 final_score 决定是否吸收这类信号，而不是把 AIHOT 作为主源。
- 本地公共 `rsshub.app` 对 XSignals 经常 302/404；GitHub Actions 中临时 RSSHub + `TWITTER_AUTH_TOKEN` 能正常启动，是观察 X 动态的主路径。
- AI 前沿的问题不是缺量，而是论文源容易刷屏；已通过 `source_caps.arxiv.org: 2` 控制。
- AI 安全的问题是有效高质量源仍偏少；补源比继续降阈值更重要。
- 金融科技需要更多官方/中文直采源；当前中文来自 Google News fallback，能保覆盖但不是长期最优解。

## This Week Acceptance Criteria
- `reports/source_audit.md` 在 daily workflow 中自动生成并提交。
- 过去 7 天 feed 的 selected unknown 源可见、可追踪。
- 当前最新日报 selected unknown 源降到 0。
- `reports/offline_eval.md` 展示 `Avg Final` 与聚类 `Merged`，下一次 daily 生成新 digest 后开始出现非空 final_score。
- AI 前沿站点展示中 arXiv 不超过 2 条，且 XSignals/官方源能进入前列。
- 安全板块 15 条中中文源不少于 6 条。
- 站点卡片按 `final_score` 优先排序，T 等级只做同分/缺分兜底。
- 测试、ruff、GitHub Pages 部署保持绿色。

## Next Actions
- 继续补 AI 安全源，优先找可 RSS 化的 AI 安全公众号、厂商安全博客、模型安全研究团队和官方公告。
- 补金融中文直采源，优先蚂蚁集团、支付宝、腾讯金融科技、财付通、数字人民币/央行相关可靠 RSS。
- 设计并试运行人工反馈机制：先做 JSONL 反馈日志 + offline eval，不直接自动改权重。
- 观察 3-5 天 GitHub Actions 结果，确认安全中文比例、AI 前沿 arXiv 上限和 XSignals 稳定性。
