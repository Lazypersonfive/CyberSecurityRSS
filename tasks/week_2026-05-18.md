# Week 2026-05-18 Worklog

## Context

上一周自动化稳定运行，但 offline eval 显示传统安全中文目标没有达成：`security` 7 日平均 `14.7/15`，中文平均 `3.3/6`，`CN OK Days = 0/7`。AI 前沿和金融科技基本稳定；AI 安全偶尔混入泛 AI/商业新闻。

## Completed This Round

### Security Chinese Source Recovery

- 参考 `zhengjim/Chinese-Security-RSS`，补入一批中文安全直采源和仍可能可用的公众号代理源。
- 新增代表源：跳跳糖、360 Netlab、清华 NISL、EVILCOS、3gstudent、李劼杰、UknowSec、SecPulse 安全脉搏等。
- 安全 `CN_News` 当前可解析源数：43。

### Security Source Caps

- 下调英文高产媒体挤占：
  - `bleepingcomputer.com: 3`
  - `thehackernews.com: 3`
  - `vulners.com: 3`
  - `infosecwriteups.com: 2`
  - `arstechnica.com: 1`
- `security.fill_score_floor` 从 `5` 调到 `4`，让中文漏洞/攻防技术候选更容易进入 reserve 池。
- `security.source_policy.max_per_source` 从 `5` 调到 `4`。

### AI Security Tightening

- 新增 `adjust_ai_security_score()`：如果条目没有 AI 安全/漏洞/攻击/防护/供应链等技术安全信号，则分数上限为 `3`。
- `ai_security.fill_score_floor` 从 `4` 调到 `5`，宁缺毋滥，避免普通 OpenAI 产品/合作新闻补位。

### Source Registry Cleanup

- 登记新增中文安全源为 `cn_expert` 或 `cn_official`。
- 处理近期入选 unknown：
  - `k8gege.org` → `cn_expert`
  - `blog.csdn.net` → `community`
  - `furutsuki.hatenablog.com` → `community`

### Feedback Loop P1

- 新增 `feedback/README.md`。
- 新增 `feedback_cli.py add`，将人工反馈 append 到 `feedback/YYYY-MM-DD.jsonl`。
- 新增 `feedback_eval.py`，生成 `reports/feedback_eval.md`。
- 当前只生成建议，不自动改 prompt、配置或源权重。

## Validation

```bash
/Users/dedsec/anaconda3/envs/work3124/bin/python -m ruff check .
PYTHONPATH=. /Users/dedsec/anaconda3/envs/work3124/bin/python tests/test_regressions.py
/Users/dedsec/anaconda3/envs/work3124/bin/python -m py_compile feedback_cli.py feedback_eval.py security_editorial.py digest_pipeline_gemini.py
```

Result: ruff clean, py_compile clean, 102 unittest pass.

`pytest` is not installed in the `work3124` environment, so validation used the existing unittest entrypoint.

## Next Observation

明早 cron 后重点看：

1. `security.selection_stats.chinese` 是否从 2-5 提升到接近 6。
2. BleepingComputer / The Hacker News 是否不再合计占据半个安全板块。
3. `ai_security` 是否仍混入泛 OpenAI 产品/合作新闻。
4. `source_audit.md` 中 security unknown 是否下降。
